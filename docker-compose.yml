---
version: '2'
services:
  postgres:
    image: postgres:9.6
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=myDB
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=1234
      - MAX_CONNECTIONS=300

